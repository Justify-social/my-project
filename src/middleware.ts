// src/middleware.ts
// Re-export the actual middleware logic from its configured location
export { default } from './config/middleware/middleware';

// Re-export the config as well
export { config } from './config/middleware/middleware'; 