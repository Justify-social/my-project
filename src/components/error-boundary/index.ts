/**
 * @deprecated This directory is deprecated and will be removed in a future release.
 * Please update imports to use the components directly from this directory for now.
 * 
 * This is a re-export file for backward compatibility.
 */

// Re-export everything from the local ErrorBoundary component file
export * from './ErrorBoundary';

// Remove default export/re-export as ErrorBoundary likely doesn't have one
// import DefaultExport from './ErrorBoundary';
// export default DefaultExport;
