/**
 * FormField Component
 * 
 * A wrapper for form inputs that provides a consistent layout with labels,
 * validation, and help text.
 */

export { default as FormField } from './FormField';
export type { FormFieldProps } from './FormField'; 