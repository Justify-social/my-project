import React from 'react'; import { Influencer } from '../../../types/influencer'; interface IndividualInfluencerCardProps { influencer: Influencer; layout: 'card' | 'row'; showMetrics: boolean; onClick: () => void; } const IndividualInfluencerCard: React.FC<IndividualInfluencerCardProps> = ({ influencer, layout, showMetrics, onClick }) => { return ( <div onClick={onClick} className={layout === 'card' ? 'bg-white shadow rounded p-4' : 'bg-white shadow rounded flex items-center p-4'}> <div className={layout === 'card' ? 'mb-2' : 'flex-1'}> <h3 className="text-lg font-bold">{influencer.name}</h3> {showMetrics && ( <div> <p className="text-sm text-gray-600">Followers: {influencer.followers}</p> <p className="text-sm text-gray-600">Engagement: {influencer.engagement}%</p> </div> )} </div> </div> ); }; export default IndividualInfluencerCard;
