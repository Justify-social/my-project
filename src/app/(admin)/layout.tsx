// Layout file for (admin)
// Simplified to pass through children directly to inherit root layout
export default function adminLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}